# kaloron-codec

Textual codec for `TypeShape` values using a JSON-like text format. Supports version-aware encoding/decoding.

## Dependencies

- `anyhow` — error handling
- `serde_json` — JSON value support for some primitives
- `kaloron` — core type-shape library
